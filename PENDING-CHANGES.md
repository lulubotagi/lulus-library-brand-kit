# Pending Changes (batch for next deploy)

## 1. Line break support in quote card textarea
- Split input on `\n` before word wrapping
- Pressing Enter in textarea = forced line break in output
- Same fix as Python quotable.py got today

## 2. (add more here before deploying)
